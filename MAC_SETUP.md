# macOS 개발 환경 설정

## ⚠️ 문제: "too many open files" 및 권한 오류

Next.js 개발 시 macOS에서 발생하는 파일 제한 및 권한 문제 해결 방법입니다.

---

## 🔧 해결 방법 1: 파일 제한 증가 (임시)

현재 터미널 세션에서만 적용됩니다:

```bash
# 파일 제한 확인
ulimit -n

# 파일 제한 증가 (10240으로 설정)
ulimit -n 10240

# 확인
ulimit -n
```

---

## 🔧 해결 방법 2: 파일 제한 증가 (영구)

### 1. `.zshrc` 파일 수정

```bash
# 홈 디렉토리로 이동
cd ~

# .zshrc 파일 편집 (없으면 생성)
nano .zshrc
```

### 2. 다음 내용 추가

```bash
# macOS 파일 제한 증가
ulimit -n 10240
```

### 3. 저장 및 적용

- `Ctrl + O` → Enter (저장)
- `Ctrl + X` (종료)
- 터미널 재시작 또는 `source ~/.zshrc` 실행

---

## 🔧 해결 방법 3: Full Disk Access 권한 부여

macOS Sequoia 이상에서 필요할 수 있습니다.

### 1. 시스템 설정 열기

`시스템 설정` → `개인정보 보호 및 보안` → `전체 디스크 접근 권한`

### 2. 터미널 앱 추가

- Cursor를 사용 중이라면: **Cursor** 앱 추가
- iTerm을 사용 중이라면: **iTerm** 앱 추가
- 기본 터미널을 사용 중이라면: **Terminal** 앱 추가

### 3. 앱 재시작

터미널/Cursor를 완전히 종료하고 다시 시작합니다.

---

## 🚀 빠른 해결 방법

### 캐시 삭제 후 재시작

```bash
# 프로젝트 디렉토리로 이동
cd /Users/ts1042mac/Desktop/테스트/Auto-promo

# .next 캐시 삭제
rm -rf .next

# node_modules/.cache 삭제
rm -rf node_modules/.cache

# 파일 제한 증가
ulimit -n 10240

# 서버 재시작
npm run dev
```

---

## 🔍 문제 확인

### 1. 파일 제한 확인

```bash
ulimit -n
# 결과: 256 (낮음) → 문제 있음
# 결과: 10240 (높음) → 정상
```

### 2. Supabase 환경 변수 확인

```bash
# .env.local 파일 권한 확인
ls -la .env.local

# 읽기 권한이 있는지 확인
cat .env.local | grep SUPABASE
```

### 3. 서버 로그 확인

서버 시작 시:
- ✅ `✓ Ready in XXXXms` → 정상
- ❌ `EMFILE: too many open files` → 파일 제한 문제
- ❌ `EPERM: operation not permitted` → 권한 문제

---

## 🎯 권장 설정 (개발 환경)

```bash
# 1. .zshrc에 추가 (영구 설정)
echo "ulimit -n 10240" >> ~/.zshrc

# 2. 현재 세션에 즉시 적용
source ~/.zshrc

# 3. 확인
ulimit -n

# 4. .next 캐시 삭제
cd /Users/ts1042mac/Desktop/테스트/Auto-promo
rm -rf .next

# 5. 서버 재시작
npm run dev
```

---

## 📞 문제가 계속되면

1. **터미널 완전 재시작**: Cursor/터미널 앱을 완전히 종료 후 재시작
2. **macOS 재부팅**: 시스템 설정이 적용되도록 재부팅
3. **node_modules 재설치**: `rm -rf node_modules && npm install`

---

## ✅ 정상 작동 확인

서버 시작 시 다음 로그가 보이면 성공:

```
▲ Next.js 14.2.35
- Local:        http://localhost:3000

✓ Starting...
✓ Ready in XXXXms
```

그리고 Supabase 연결 로그:

```
✅ Supabase 연결됨
📊 Supabase 클라이언트 상태: { hasClient: true, ... }
```

